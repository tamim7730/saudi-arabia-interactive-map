# متغيرات البيئة للمشروع
# انسخ هذا الملف إلى .env وقم بتعديل القيم حسب الحاجة

# منفذ الخادم
PORT=3000

# بيئة التشغيل (development, production, test)
NODE_ENV=development

# إعدادات قاعدة البيانات (إذا كانت مطلوبة مستقبلاً)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=saudi_map
# DB_USER=saudi_user
# DB_PASSWORD=your_password

# مفاتيح API الخارجية (إذا كانت مطلوبة)
# GOOGLE_MAPS_API_KEY=your_google_maps_key
# WEATHER_API_KEY=your_weather_api_key

# إعدادات الأمان
# JWT_SECRET=your_jwt_secret_key
# SESSION_SECRET=your_session_secret

# إعدادات التخزين السحابي (إذا كانت مطلوبة)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=your_bucket_name

# إعدادات البريد الإلكتروني (إذا كانت مطلوبة)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_email_password

# إعدادات التسجيل
LOG_LEVEL=info

# حد الطلبات (عدد الطلبات لكل 15 دقيقة)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# حجم الملفات المرفوعة (بالميجابايت)
MAX_FILE_SIZE=50

# مجلد رفع الملفات
UPLOAD_DIR=uploads

# تفعيل ضغط الاستجابات
ENABLE_COMPRESSION=true

# تفعيل إعدادات الأمان
ENABLE_HELMET=true

# إعدادات CORS
CORS_ORIGIN=*

# إعدادات Redis (للتخزين المؤقت)
# REDIS_URL=redis://localhost:6379

# إعدادات SSL (للإنتاج)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# إعدادات التخزين المؤقت
CACHE_TTL=3600

# إعدادات التحليلات (اختياري)
# GOOGLE_ANALYTICS_ID=GA_TRACKING_ID

# إعدادات النسخ الاحتياطي
BACKUP_ENABLED=false
BACKUP_INTERVAL=24

# إعدادات المراقبة
MONITORING_ENABLED=true
HEALTH_CHECK_INTERVAL=30

# إعدادات Render (للنشر)
RENDER_EXTERNAL_URL=https://your-app-name.onrender.com